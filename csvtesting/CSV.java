package csvtesting;

public class CSV {
    // inject a CSV file into the program, containing the following information:
    // workouts, exercises, tools

    // need to read the CSV file and store the information in a data structure
    // that can be used to access the information
    CSVReader reader = new CSVReader();
}
